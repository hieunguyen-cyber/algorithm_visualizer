{
  "name": "automatic-question-generator",
  "version": "1.0.0",
  "description": "Algorithm Visualizer",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -R frontend/* public/"
  }
}
